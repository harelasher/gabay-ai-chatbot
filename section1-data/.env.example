# Required for Section 1
OPENAI_API_KEY=your_openai_api_key_here

# Docker PostgreSQL (matches docker-compose.yml credentials)
# Port 5433 avoids conflict with a local PostgreSQL on the default 5432
DATABASE_URL=postgresql://gabay:gabaypass@localhost:5433/gabay_chatbot
